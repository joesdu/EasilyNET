<mica:MicaWindow x:Class="WpfApp.Test.MainWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:mica="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:local="clr-namespace:WpfApp.Test" 
      xmlns:viewModels="clr-namespace:WpfApp.Test.ViewModels" 
      d:DataContext="{d:DesignInstance viewModels:MainWindowModel}"
      mc:Ignorable="d"
      Title="MainWindow" Height="600" Width="800"
      WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Message}"></TextBlock>
        <mica:Button Grid.Row="1" Margin="10" FontSize="24" Click="OnBtnClick" Content="ClickMe" />
    </Grid>
</mica:MicaWindow>
