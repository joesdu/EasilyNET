# ADR-0005: 快照触发阈值策略

## 状态

Accepted

## 背景

日志持续增长会拉高恢复时间与存储成本；过度快照又会放大 I/O 压力。

## 决策

- 使用 `SnapshotThreshold` 作为主阈值。
- 在慢节点追赶场景，结合 lag 判断触发 `InstallSnapshot` 降级路径。

## 影响

- 优点：可控压缩频率，保障恢复可预期。
- 代价：阈值需按命令大小、磁盘性能和恢复目标调优。
