# ADR-0002: 成员变更策略（单节点请求 + joint-consensus 执行）

## 状态

Accepted

## 背景

成员变更是 Raft 高风险路径。直接多节点并发变更实现复杂且易破坏可用性与安全性。

## 决策

- API 层仅允许一次增/删一个节点。
- 引擎层按 joint-consensus 两阶段执行：`C_old,new -> C_new`。
- 变更窗口禁止并发配置变更请求。

## 影响

- 优点：降低实现和运维风险，语义清晰。
- 代价：大规模扩缩容需多次串行执行。
